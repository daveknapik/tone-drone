function SequenceManager() {
  // create the loop
  // keep track of currentBeat
  // hold the sequence and synth objects for each oscillator
  // give the oscillators a way to change the sequences and synths so that they can be used here

  return <div>hello from SequenceManager</div>;
}

export default SequenceManager;
